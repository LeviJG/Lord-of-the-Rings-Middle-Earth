{
  "__comment": "The block is RenderShape.INVISIBLE, so every state maps to an empty model; the block entity renderer draws the rack.",
  "variants": {
    "": { "model": "minecraft:block/air" }
  }
}
